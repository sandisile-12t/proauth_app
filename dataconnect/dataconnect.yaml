specVersion: "v1"
serviceId: "proauth"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "proauth-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
